Timed out retrying after 60000ms: `cy.contains()` failed because it requires a DOM element, window or document.

No elements in the current DOM matched your query:

  > cy.get(table tbody tr).contains(Style_zb43mu).closest(tr).find(div.n-button-content)